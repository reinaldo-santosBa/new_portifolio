import styled from 'styled-components';

export const menu = styled.nav`
    display: flex;
    flex-direction: row;
    padding: 2rem 8rem;
    align-items: center; 
    justify-content: space-between;
    div{
        display: flex;
        flex-direction: row;
        align-items: center; 
        gap:1rem;
    }
`;